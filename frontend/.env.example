# URL du backend FastAPI (utilisée en production, pas en dev)
# En dev, le proxy Vite redirige automatiquement /api vers http://localhost:8000
VITE_API_URL=/api

# URL du broker MQTT (si on veut afficher le statut MQTT en temps réel)
VITE_MQTT_URL=ws://localhost:9001
